<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_marginStart="8dp"
    android:layout_marginEnd="8dp"
    android:layout_gravity="center"
    android:layout_height="wrap_content"
    android:id="@+id/custom_toast_layout_light"
    android:orientation="vertical"
    android:background="@drawable/toast_background_light"
    >

    <ImageView
        android:id="@+id/custom_toast_image"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginStart="12dp"
        android:layout_marginLeft="12dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="12dp"
        android:paddingTop="20dp"
        android:paddingBottom="20dp"
        android:contentDescription="Hello world"
        android:background="@drawable/pill_bottle_color_2"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <TextView
        android:id="@+id/custom_toast_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        android:text="This is a custom Simpill message.\n\nI hope that you are having a good day :)"
        android:textColor="@color/white"
        android:fontFamily="@font/truenoreg"
        android:textSize="14sp"
        android:textAlignment="center"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="@+id/custom_toast_image"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/custom_toast_image"
        app:layout_constraintTop_toTopOf="@+id/custom_toast_image" />
</androidx.constraintlayout.widget.ConstraintLayout>